[workspace]
members = ["crates/anchor"]
resolver = "3"


[profile.dev]
incremental = true
codegen-units = 256 # More units = faster, but less optimized binary
debug = 0 # 0 or 1 for faster build

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
# panic = "abort"
strip = true
